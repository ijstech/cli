{
    "name": "demo-lib",
    "version": "0.0.1",
    "main": "lib/index.js",
    "types": "types/index.d.ts",
    "plugin": "dist/index.js",
    "pluginTypes": "dist/index.d.ts",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "test": "npx @ijstech/cli test test/*.test.ts",
        "build": "rm -rf dist && rm -rf lib && rm -rf types && rm -rf pluginTypes && npx @ijstech/compiler"
    },
    "devDependencies": {
        "@ijstech/cli": "^0.1.38",
        "@ijstech/compiler": "^0.1.107",
        "@types/node": "^20.11.25"
    }
}
