{
    "name": "demo2",
    "version": "0.1.0",
    "engines": {},
    "dependencies": {
        "@ijstech/eth-wallet": "^0.1.19"
    },
    "devDependencies": {
        "@ijstech/cli": "^0.1.6",
        "@ijstech/compiler": "^0.1.7",
        "@ijstech/components": "^0.1.17"
    },
    "scripts": {
        "build": "rm -rf dist && npx @ijstech/compiler",
        "serve": "npx @ijstech/cli serve",
        "dev": "npm run build && npm run serve"
    }
}
