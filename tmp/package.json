{
    "name": "demo2",
    "version": "0.1.0",
    "engines": {},
    "dependencies": {
        "@ijstech/eth-wallet": "^0.1.18",
        "moment": "^2.29.4"
    },
    "devDependencies": {
        "@ijstech/cli": "^0.1.5",
        "@ijstech/compiler": "^0.1.7",
        "@ijstech/components": "^0.1.16"
    },
    "scripts": {
        "build": "rm -rf dist && npx compiler",
        "serve": "npx cli serve",
        "dev": "npm run build && npm run serve"
    }
}
